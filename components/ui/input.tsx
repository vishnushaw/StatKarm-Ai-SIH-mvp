import * as React from 'react'; import { cn } from '@/lib/utils';
const Input = React.forwardRef<HTMLInputElement, React.ComponentProps<'input'>>(({ className, ...props }, ref) => <input className={cn('h-10 w-full rounded-lg border border-[#dce4f0] bg-white px-3 text-sm text-ink outline-none placeholder:text-slate-400 focus:border-brand focus:ring-2 focus:ring-[#315bc620]', className)} ref={ref} {...props} />); Input.displayName = 'Input'; export { Input };
